# ==================== REQUIRED ====================

# JWT Secret for authentication (min 32 characters, change in production!)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# Encryption secret for credential vault (AES-256-GCM requires 32 bytes)
# Generate with: openssl rand -hex 16
ENCRYPTION_SECRET=your-32-byte-encryption-secret-key-change-this-in-production

# Database (PostgreSQL)
# Local development: use docker-compose or local PostgreSQL
# Railway: automatically injected as DATABASE_URL
DATABASE_URL="postgresql://user:password@localhost:5432/afflyt?schema=public"

# ==================== SERVER ====================

PORT=3001
NODE_ENV=development

# App URL (used for email links)
APP_URL=http://localhost:3000

# ==================== EMAIL (Resend) ====================

# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Sender email (must be verified domain in Resend)
# For testing, use: onboarding@resend.dev
FROM_EMAIL=noreply@afflyt.io
FROM_NAME=Afflyt Pro

# ==================== KEEPA API ====================

KEEPA_API_KEY=your-keepa-api-key-64-characters-here
KEEPA_DOMAIN=IT
KEEPA_MAX_RETRIES=3
KEEPA_TIMEOUT_MS=30000

# ==================== CORS ====================

# Comma-separated list of allowed origins (production only)
CORS_ORIGINS=https://afflyt.io,https://www.afflyt.io

# ==================== OPTIONAL ====================

# SendGrid (legacy, prefer Resend)
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxx
