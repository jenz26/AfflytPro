[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "npx prisma generate --schema=./prisma/schema.prisma"]

[start]
cmd = "npx prisma migrate deploy --schema=./prisma/schema.prisma && node dist/app.js"
