import './globals.css';

export const metadata = {
  title: 'Afflyt Pro',
  description: 'Amazon Associates Management Platform',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
