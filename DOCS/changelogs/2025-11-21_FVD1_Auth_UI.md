# Changelog - FVD 1: Vertical Development Authentication & UI Core

**Date:** 2025-11-21
**Version:** 0.1.0
**Author:** Antigravity (AI Assistant)

## Summary
This release establishes the foundational architecture for AfflytPro (Monorepo: Fastify + Next.js) and implements the first vertical slice: Authentication and the "Cyber Intelligence" Visual Identity.

## 🚀 New Features

### Architecture
- **Monorepo Structure**: Initialized `apps/api` and `apps/web` workspaces.
- **Database**: Setup SQLite with Prisma ORM.
- **Backend**: Fastify server baseline with CORS and JWT support.
- **Frontend**: Next.js 14+ (App Router) with Tailwind CSS.

### Authentication (Backend)
- **Database Models**: Added `User` and `ApiKey` schemas.
- **API Routes**:
    - `POST /auth/register`: User registration endpoint (now supports password hashing with `bcrypt`).
    - `POST /auth/login`: Authentication endpoint (supports both Magic Link and Password).
    - `GET /auth/profile`: Protected user profile endpoint.
- **Security**: 
    - Implemented JWT middleware for route protection.
    - Added `bcryptjs` for secure password hashing.

### UI/UX (Frontend)
- **Design System**: Implemented "Cyber Intelligence" theme:
    - Custom Color Palette (`afflyt-cyan`, `afflyt-plasma`, `afflyt-dark`).
    - Fonts: Space Grotesk (Headers), Inter (Body), JetBrains Mono (Data).
    - **Fix**: Updated Tailwind configuration to support v4 syntax.
- **Core Components**:
    - `CyberButton`: Custom interactive button with corner cuts and glow effects.
    - `GlassCard`: Glassmorphism container component.
    - `FuturisticSidebar`: Responsive sidebar with navigation, Dark Mode, and i18n toggles.
- **Pages**:
    - Login Page (`/auth/login`): Implemented Dual-Mode Login (Magic Link + Password) with toggle UI.
    - Root Page (`/`): Added redirect to `/auth/login`.

## 🛠 Technical Details
- **Dependencies Added**:
    - API: `fastify`, `@fastify/jwt`, `@fastify/cors`, `prisma`, `@prisma/client`.
    - Web: `lucide-react`, `clsx`, `tailwind-merge`.
- **Configuration**:
    - Tailwind configured with custom colors and fonts.
    - Global CSS variables for theming.

## ✅ Verification
- **Build Status**:
    - `apps/api`: Build Successful.
    - `apps/web`: Build Successful.
- **Manual Testing**:
    - UI components verified against design specs.
    - Auth flow simulated successfully.
